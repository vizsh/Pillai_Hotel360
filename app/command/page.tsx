import { CommandCenter } from "@/components/command/CommandCenter";

export const metadata = { title: "Command Center · Smart Resort 360" };

export default function CommandPage() {
  return <CommandCenter />;
}
